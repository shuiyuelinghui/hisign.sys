import ApproveController from '../controllers/approve';

const ApproveComponent = {
    templateUrl: './templates/modules/approve.html',
    controller: ApproveController,
    controllerAs: 'approve'
};

export default ApproveComponent;
